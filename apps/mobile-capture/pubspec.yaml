name: mobile_capture
description: Aesthetica companion capture app for Meta Ray-Ban DAT stream
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  flutter_voice_processor: ^1.1.2
  http: ^1.2.2
  image: ^4.2.0
  path_provider: ^2.1.5
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
